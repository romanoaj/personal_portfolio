# Your résumé goes here

Add your résumé as:

    resume.pdf

The "Download Résumé" button in the site (js/content.js, under the
`resume` entry) already points to `assets/documents/resume.pdf`, so
dropping a correctly-named file here is all that's needed — no code
changes required.
